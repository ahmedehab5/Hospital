@model IEnumerable<dynamic> // Assuming 'dynamic' as the model type based on your example

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <!-- You could add a dropdown or select option here to choose a specific patient -->
    <a asp-action="Create" asp-route-patientId="e105a10c-2289-41ad-94cb-472d0764c889">Create New Appointment</a>

</p>
<p>
    <a asp-action="HistoryForPatient" asp-route-patientId="e105a10c-2289-41ad-94cb-472d0764c889">View Patient History</a>
</p>
<p>
    <a asp-action="UpcomingAppointmentsForPatient" asp-route-patientId="e105a10c-2289-41ad-94cb-472d0764c889">View Patient Upcoming Appointments</a>
</p>
<p>  <a asp-action="HistoryForDoctor" asp-route-doctorId="D002">View Doctor History</a>
</p>
<p>
    <a asp-action="UpcomingAppointmentsForDoctor" asp-route-doctorId="D002">View Doctor Upcoming Appointments</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Doctor</th>
            <th>Patient</th>
            <th>Specialization</th>
            <th>Date</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.DoctorFullName</td>
                <td>@item.PatientFullName</td>
                <td>@item.Specialization</td>
                <td>@item.Date.ToString("MM/dd/yyyy")</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
