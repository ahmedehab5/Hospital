@model IEnumerable<Hospital.Models.Appointment>

@{
    ViewData["Title"] = "Appointment History for Patient";
}

<h1>Appointment History for @ViewData["PatientName"]</h1>
<table class="table">
    <thead>
        <tr>
            <th>Doctor</th>
            <th>Specialization</th>
            <th>Date</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var appointment in Model)
        {
            <tr>
                <td>@appointment.Doctor?.FirstName @appointment.Doctor?.LastName</td>
                <td>@appointment.Specialization</td>
                <td>@appointment.Date.ToString("g")</td>
                <td>
                    <a asp-action="Details" asp-route-id="@appointment.Id">Details</a>
                </td>
            </tr>
        }
    </tbody>
</table>

<div>
    <a asp-action="Index">Back to Appointments</a>
</div>
